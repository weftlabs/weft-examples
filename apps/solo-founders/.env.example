# Create a buyer key at https://weft.network/dashboard/buyer/api_keys
WEFT_API_KEY=wk_replace_me

# Set to 1 only if this deployment should spend the key on public traffic.
# Leave unset for local-only (the default).
# WEFT_PUBLIC=1
